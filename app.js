/* ==========================================================================
   SEPUTARMODELAI — app.js
   Logika HANYA untuk beranda (navbar, scroll effect, mobile menu).
   ========================================================================== */

window.SMAI.onReady(window.SMAI.initNavbar);
