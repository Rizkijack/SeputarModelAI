{
  "name": "seputarmodelai",
  "version": "1.0.0",
  "description": "Website komparasi seluruh model AI — tayang live di Vercel.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "license": "ISC",
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.10",
    "jsdom": "^26.1.0",
    "vitest": "^4.1.10"
  }
}
